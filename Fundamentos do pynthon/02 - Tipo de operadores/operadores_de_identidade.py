#operadores de identidade

#is not = verifica se dois objetos não são o mesmo objeto
#is = verifica se dois objetos são o mesmo objeto

number1 = 10
number2 = 10

print(number1 is not number2)  # False, porque ambos são o mesmo objeto
print(number1 is number2)      # True, porque ambos são o mesmo objeto